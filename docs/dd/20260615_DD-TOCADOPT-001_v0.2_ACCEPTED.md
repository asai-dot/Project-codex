# DD-TOCADOPT-001 — 統一TOC採用ルール v0.2 ACCEPTED (design-only)

- 日付: 2026-06-15
- status: **accepted design (design-only)**。owner ratify 済 (2026-06-15)。
- GPT 監査: **DDTOCADOPT_PASS_WITH_NOTES** (from_gpt Box 2286066609478)。
- supersedes: v0.1 REQUEST (`20260615_DD-TOCADOPT-001_unified_toc_adoption_REQUEST.md`)。
- 実体: Project-codex branch `claude/legallib-integration-design-Jgrtf`。
  classifier=`scripts/edition_identity_v2.py` / 統合policy DRAFT=`data/toc_merge_policy_unified_DRAFT.json` /
  回帰証跡=`handoff/legallibjoin_v0.3.1_phase0_20260615/edition_identity_v2_regression.md`。

> **幹の原則 (PASS)**: 源どうしの勝ち負けでなく、本/ノード単位で最良の中身を出所記録つきで
> 合成して採用する。priority ラダーは tie-break 専用。

---

## 1. owner 裁定 (OQ-1〜4)

- **OQ-1 = legallib > toc_pdf を維持**。
  - v0.1 たたき台の `toc_pdf > legallib` は Fork1 順の無検証持ち込みで、本流 v2 が
    D-5(2026-06-11 owner ratify)で確定した `legallib > toc_pdf`(逆順=誤記と明記) と衝突していた。
  - 判断根拠: ①`toc_pdf` は `publisher`(別源・上位)とは別の **PDF 抽出レイヤ**であり、
    legallib は Phase0 実測でクリーン(空タイトル0.01%/ページ被覆100%/適切なネスト)。同粒度なら
    クリーンな源を採るのが妥当。②既 ratify 決定を逆転する merit が無い。③新原則で tie-break 専用=影響小。
- **OQ-2** = granularity_guard 既定 0.2、ただし **source 別に再較正可能な policy parameter**。
- **OQ-3** = 弁コム↔legallib 同一本は粒度優先で可、ただし **provenance_origin 同一の票水増し禁止**。
- **OQ-4** = Fork1 policy は統合 DRAFT が既存 dry-run を完全再現した後に廃止 (**即廃止不可**)。

---

## 2. Required note を採用ルールへ反映 (GPT 監査)

- **note(統合policy)**: 統合 policy 作成は「既存 projection dry-run 完全再現」を **最初の gate**。
  再現前に新採用関数へ切替えない → §4 gate 1。
- **note 2 (Step1)**: **ISBN 一致だけで同一 manifestation 確定にしない**。版番号一致でも
  page_count / publisher / year が大きく食い違えば review(INSUFFICIENT)へ。→ `edition_identity_v2` に実装。
- **note 5 (Step3)**: `append_missing_only` は **node invention 禁止**。補完ノードは必ずいずれかの
  source snapshot に存在し、`locator / source_hash / page_basis` を持つ。→ §4 gate 4/5。
- **note 4 (Step4)**: consensus は独立 source 数でなく **`provenance_origin` 単位**で数える。
  弁コムと legallib の再配信で票を二重計上しない。→ §4 gate 6。

5 ステップ本体 (同一性ゲート→基底選択→ノード補完→保護合議→記録) は v0.1 §3 のまま。
上記 note を各 Step に織り込んだ実装契約が `data/toc_merge_policy_unified_DRAFT.json`。

---

## 3. 実装済 (report-only / 本番未反映)

- **`edition_identity_v2.py`**: 版番号抽出 + 核タイトル包含 + 年差±1許容 + 版番号一致時の年無視
  + Required note 2(page/publisher 不一致は review)。tests 9件 PASS。
- **gate_edition_identity_phase0_regression 充足** (`edition_identity_v2_regression.md`):
  Phase0 2,082 対で別版疑い **v1 344 → v2 72**。
  - 確実な別版(版番号衝突) を SUSPECTED に **26/26**。
  - 装飾/副題差を RESOLVED_SAME 通過 **203/210**(残7は phase0 が誤分類した真の別版/別巻を
    v2 が年差信号で救出=精度向上)。
- **統合 policy DRAFT** (`toc_merge_policy_unified_DRAFT.json`): v2 を幹に Fork1 の simple_only/
  edition gate を吸収。priority は OQ-1 確定順。granularity 源別 param 化。votes=provenance_origin。

---

## 4. 実装前 required gates (GPT 指定・本番反映の前提)

1. `gate_policy_unification_reproduces_existing_projection` — 統合 policy で既存
   tocattach_projection_dryrun(631クラスタ/116,727ノード)を完全再現(hash 入力順非依存・基底源分布不変)。
2. `gate_edition_identity_phase0_regression` — 別版26件除外・装飾226件同一通過・unresolved/suspected は review。**✓ 達成 (§3)**。
3. `gate_no_rich_to_shallow_degradation` — 詳細→浅い劣化0(granularity_guard 実効)。
4. `gate_node_provenance_complete` — 全採用/補完ノードに source_system/provenance_origin/locator/page_basis/source_hash。
5. `gate_append_missing_no_node_invention` — 補完ノードは必ず source snapshot に存在。
6. `gate_votes_by_provenance_origin` — votes は provenance_origin 単位・重複配信の二重計上禁止。
7. `gate_report_only_no_write` — canonical/snapshot/source/DB 書込み0。**✓ 現状維持**。

---

## 5. release boundary

- **許可済(実施可)**: design rule 採用 / owner ratify(design-only) / 統合policy案作成 /
  classify_edition_identity 強化 / report-only projection dry-run。
- **HOLD**: production apply / canonical projection apply / RDB write / source snapshot mutation /
  policy本番切替 / Fork1即廃止 / unresolved manifestation の合議参加 / foreign-source auto rebasing 拡大。

---

## 6. 次の一手

1. `gate_policy_unification_reproduces_existing_projection` を本流(ALOBookDX)の
   projection harness で実行(統合 DRAFT で 631 クラスタ再現)。← 本番 policy 切替の前提。
2. gate 3/4 を report-only projection で確認。
3. 全 gate 緑 + 本記録を owner 最終 ratify → 初めて policy 切替 / apply を検討。
