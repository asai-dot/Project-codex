---
request_id: 20260624_lawtime_v0.2.4_placement_DDLAWTIME
topic: lawtime / lawsubtrans — placement patch v0.2.4（C-option 実装監査）
gate: DDLAWTIME
version: v0.2.4 (placement patch — C-option)
supersedes: 20260624_lawtime_supabase_placement_DDLAWTIME (相談→本実装で着地)
responds_to_result:
  - file_id: 2305621550301   # RESULT DDLAWTIME_PLACEMENT_PASS_WITH_NOTES (C-option 採用)
  - file_id: 2304093933196   # RESULT v0.2.3a PASS_WITH_NOTES (should_fix #1/#2/#3)
current_governing_result: DDLAWTIME_PLACEMENT_PASS_WITH_NOTES
git_commit: 90f9d8f   # placement_v0.2.4 一式 + design doc + README/COVERAGE
git_branch: claude/lawsubtrans-production-deploy-b15nmp
git_pr: https://github.com/asai-dot/Project-codex/pull/34
result_expected_filename: 20260624_lawtime_v0.2.4_placement_DDLAWTIME_RESULT.md
target_mode: design_audit   # 本番 apply ではない。C-option 実装の ratify 可否監査。
source_hash: 79f300d60818   # sha1(000_external + 100_schema + 200_gates + 300_serving)[:12]
review_scope: |
  migrations/lawtime/placement_v0.2.4/000_external_dependency_d1law_taikei.sql  (母屋 fixture, smoke only)
  migrations/lawtime/placement_v0.2.4/100_lawtime_schema.sql   (URI identity / citation_temporal side-table / resolver / eval append-only / unresolved_queue / revision_mapping。search_path 無し)
  migrations/lawtime/placement_v0.2.4/200_gates.sql            (house style v_gate_lawtime_*_v20260624 ×8)
  migrations/lawtime/placement_v0.2.4/300_serving.sql          (serving current/resolved view + claim_support_decision truth table)
  migrations/lawtime/placement_v0.2.4/{verify_dry_run,seed_clean,sample_resolver,violations}.sql + smoke_placement.sh
  docs/dd/DD-LAWTIME-001_v0.2.4_placement.md                   (C-option 設計書)
regression_anchors: |
  - self-contained ローカル構造スモーク PASS（smoke_placement.sh, PG16, 課金なし）:
    8 gate 全空（C-INT×2 + P0-2/3/4 + N-1/2/3/4）/ 仕込み違反 8 件全検知（各 1）/
    golden resolver 6 行（半開区間・施行前 null を確認）/ serving truth table 整合 /
    guard 5 件全 RAISE（eval append-only ×2・二段 INLINE CHECK・母屋 FK・URI CHECK）。
status: answered   # RESULT=DDLAWTIME_V024_PLACEMENT_PASS_WITH_NOTES (file_id 2306481004211, 2026-06-25)。
                   # C-option design ratify / materialize branch dry-run へ GO（local/fixture/branch 範囲）。
                   # 本番 apply / Supabase 本番反映 / canonical / claim_support は HOLD 継続。
                   # 3 Notes（lawref 分離 / CASCADE 削除運用 / C-INT-1,2 必須受入）は v0.2.4 設計書 §11 に反映。
---

# GPT Pro 監査依頼: DD-LAWTIME v0.2.4 — placement C-option 実装

配置相談 RESULT `DDLAWTIME_PLACEMENT_PASS_WITH_NOTES`（file_id 2305621550301）の C 案裁定と
8 blocking notes を、**design スコープ内**（local SQL / gate / serving / smoke / 設計書）で実装した。
materialize / production apply / canonical / claim_support serving は依然 **HOLD 維持**。

## blocking notes（RESULT §4）への対応

| # | blocking note | 対応 |
|---|---|---|
| 1 | `lawtime.alo_edges` スタンドインの撤回 | 廃止。citation 本体は `d1law_taikei.alo_edges`（母屋）。lawtime は alo_edges を持たない |
| 2 | search_path 追記の撤回 | `ALTER DATABASE … search_path` 削除。全参照を明示 schema 修飾 |
| 3 | URI identity への寄せ直し | `alo:law:jp:` / `alo:lawrev:jp:` / `alo:lawprov:jp:`。各 URI 列に接頭辞 CHECK |
| 4 | 既存 `d1law_taikei.alo_edges` 影響範囲 inventory | alo_edges に**列を足さず・触らない**（side-table + FK 参照のみ）。DDL 影響ゼロを設計書 §8 に明記 |
| 5 | edge_type 語彙を DDLAWREF と整合 | citation 述語は v0.2.3 の 3 値のみ持ち越し、200_gates 冒頭で集中定義＋DDLAWREF 所有を明記。拡張は未採用 |
| 6 | temporal side-table の FK / reference policy | `citation_temporal.edge_id` PK→母屋 FK ON DELETE CASCADE、1:1 不変条件、二段 INLINE CHECK。設計書 §3 |
| 7 | serving 統合 gate の truth table 化 | `serving.lawtime_claim_support_decision`（resolved/status_ok/caveat_ok/eval_present→lawtime_serve）。設計書 §5 |
| 8 | production apply / materialize は別ゲート | timestamp migration 形態を設計書 §8 に記載しつつ apply は HOLD 継続 |

## C-option 物理配置（設計書 §1）
- `d1law_taikei`（母屋）= canonical URI typed-edge `alo_edges`。citation は `edge_type` 行で表現。
- `lawtime`（作業棟）= work/revision/provision 時間軸・succession・revision_mapping・
  temporal eval(append-only)・unresolved_queue・**citation_temporal side-table**・resolver。
- `serving`（出口）= current/resolved view + claim_support truth table。
- `public` 不使用 / `alo-connect` 不使用（動的DB予約・owner 確定）。

## v0.2.3a should_fix（file_id 2304093933196 §2）への対応
1. R 列の置換可能性 → `COVERAGE.md` 冒頭に明記（v0.2.4 の `(recon: R)` CHECK にも適用）。
2. gate 名 + 失敗時 owner action → `README.md` の gate 一覧表に転記。
3. sample resolver の golden 固定 → `placement_v0.2.4/sample_resolver.sql`（6 行固定、diff 可能）。

## 確認してほしい点
1. C-option の 8 blocking notes が**設計意図どおり閉じている**か（特に #1 撤回 / #6 side-table FK policy /
   #7 truth table に漏れ・偽陰性がないか）。
2. citation_temporal の **1:1 不変条件**（citation edge ↔ side-table）を gate（C-INT-1/2）で
   0 件保証する設計で十分か。INLINE 二段 CHECK が「全 side-table 行は citation edge」前提に依存する点の是非。
3. serving の claim_support truth table が、最終判断（d1taxo pending 除外 ∧ lawsubtrans accepted∧evidence）と
   **責務分離**できているか（lawtime が他層を重複実装していないか）。
4. URI scheme（work/revision/provision）と接頭辞 CHECK が canonical identity として妥当か。
   succession で law_id が変わる work の URI anchor の取り方（§2）に懸念はないか。
5. これで **materialize branch dry-run → owner ratify** に進めてよい水準か（NEED_MORE なら不足を明示）。
6. （不変）production apply / Supabase materialize / canonical / claim_support serving /
   DDLAWREF edge_type 確定 / lawsubtrans 接続張り替え は本 REQUEST の対象外＝HOLD 継続で正しいか。

## 監査対象（commit 90f9d8f）
- repo `migrations/lawtime/placement_v0.2.4/*` + `docs/dd/DD-LAWTIME-001_v0.2.4_placement.md`
- 参考: 配置相談 REQUEST/RESULT、v0.2.3a RESULT、正本 patch doc `docs/dd/DD-LAWTIME-001_v0.2.3_production_patch.md`

## 守秘
スキーマ名・列名・状態語彙・件数レベルのみ。実依頼者データ無し。
