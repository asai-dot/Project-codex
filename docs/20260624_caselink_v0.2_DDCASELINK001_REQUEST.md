---
request_id: 20260624_caselink_v0.2_DDCASELINK001
topic: case
gate: DDCASE
status: queued
source_ref: docs/DD-CASELINK-001_commentary_to_case_link_extraction_draft_v0.2_20260624.md (repo PR #26 / Box upload は owner GO + Wave57 MAP_UPDATE 待ち)
related_targets:
  - scripts/case_link_extract.py (記事→mention 決定部)
  - scripts/case_link_map.py (mention→alo_edges 写像・決定的)
  - scripts/case_link_eval.py + app/data/case_identity/case_link_gold_template.jsonl (リンク精度)
  - scripts/case_vocab.py (link層語彙=35_link_layer ミラー / 精度目標)
  - scripts/test_case_link_{map,extract,eval}.py (green) / test_case_consistency.py (link ゲート) / test_case_pipeline_e2e.py (⑥ 連結)
parent_accepted: DD-CASE-001 / DD-CASEID-001/002/003 (accepted v1.0) / 35_link_layer (alo_edges 正典)
result_expected_filename: 20260624_caselink_v0.2_DDCASELINK001_RESULT.md
---

# 20260624 DD-CASELINK-001 評釈→判例リンク抽出 / role typing — GPT Pro 意味監査 REQUEST

- gate: **DDCASE** / version: v0.2 / 起票: 2026-06-24 / 番頭: Claude Code (remote)
- RESULT 先頭行ラベル: `DDCASE_PASS` / `DDCASE_PASS_WITH_NOTES` / `DDCASE_MODIFY_REQUIRED` / `DDCASE_FAIL` / `DDCASE_NEED_MORE`
- 目的: 雑誌・文献の**本文**から該当判例を取り出し、評釈と判例を型付きで繋いで**意味層を厚くする**。難所「1記事:N判例(評釈と判例が1:1でない)」を、フラットなN本の同格エッジにせず正典 alo_edges の型付きエッジに載せる。

## 0. 経緯
- v0.1 は独自役割語彙(annotates/cites_* 造語・Tier A/C)を新設しており、正典 `35_link_layer` が既に持つ commentary→case 語彙(`evaluates`/`review_chain`/`compares` + `assertion_mode`)と**ドリフト**していた(自己ドリフトを検出)。
- v0.2 で正典語彙へ**全面 crosswalk**して是正。確信度は独自 Tier でなく `assertion_mode`(vendor_explicit / vendor_implicit+strength=implicit)で表現。`llm_inferred` 直書きは PoC DB制約で禁止。

## 1. 監査対象
1. **DD-CASELINK-001 draft v0.2**(repo): 役割→正典 edge_type crosswalk(§1)、確信度=assertion_mode(§2)、記事タイプ条件分け(§3)、主検出シグナル優先順位(§4)、抽出パイプライン(§5)、正典整合(§6)、正典反映 queue(§7)、写像決定表(§8.1)。
2. **実装**(repo, read-only): `case_link_extract.py`(役割分類・引用解決度) → `case_link_map.py`(mention→alo_edges 型付け・決定的) → `case_link_eval.py`(edge_type別 precision/stance正解率)。`case_vocab.py` に link層語彙を正本ミラー。`test_case_consistency.py` が「emit ⊆ 正典・llm_inferred 禁止」を恒久ゲート化。`test_case_pipeline_e2e.py` ⑥ で本文採掘エッジが①〜④の canonical case に接続することを green 実証。

## 2. 求める判定(重点)
- (a) **正典語彙への crosswalk が十分か**: 独自 edge_type を作らず `evaluates`/`review_chain`/`compares` に載せ、確信度を `assertion_mode`(masthead=vendor_explicit→auto / 本文=vendor_implicit strength=implicit→review)で表す設計は、誤 `evaluates`(評釈対象の取り違え)を防ぐのに妥当か。
- (b) **1記事:N の役割モデル**: 主(評釈対象=`evaluates` 原則1)/従(`compares`+stance)に型付けし、論文は主を作らず `central_case` ヒント止め。主検出シグナル優先順位(masthead構造 > 位置 > 共参照 > 頻度)に穴は無いか。
- (c) **同旨/反対の保存方式**: 新 edge_type を増やさず `alo_edges.stance` qualifier 列(supporting/contrasting/neutral)で保存する決定(`35_link_layer §11 OPPOSES` と整合)が妥当か。正典反映は DDCASE 通過後の owner 手順。
- (d) **llm_inferred 禁止下の境界**: LLM は候補提示まで・エッジ化は vendor_*/human のみ、本文由来は cite_gate 通過後に確定、という境界で本文採掘の価値を取りこぼさないか。fail-closed(未知シグナル非エッジ化)の副作用(recall 損)の評価。
- (e) **precision 目標**の水準: `evaluates`/`review_chain`=0.97(誤リンク最有害)・`compares`=0.90・stance=0.85 は適切か。

→ verdict を `DDCASE_<LABEL>` で先頭行に。PASS/PASS_WITH_NOTES → 浅井先生 ratify → Mac CC で D1-LIC 5,475 の本文採掘・実 `evaluates` 精度計測 → 通過後に正典(`35_link_layer §2 stance列`/§6/`33_magazine §4`)へまとめて反映。

## 3. 自己申告 / HOLD
- deterministic_self_verification = **fixture-level done**: extract→map→eval、consistency(link ゲート)、e2e ⑥ すべて green(全14テスト)。
- corpus-level = **Mac CC**: citation-span 検出器の実体(正規表現＋符号正規化)、D1-LIC 5,475 本文採掘、実 evaluates 精度は未了。合成 gold の precision 1.0 は**自己無矛盾の確認に過ぎない**。
- 守秘: 設計・合成データ・公開メタのみ。実データ不使用。
- 実装影響: **設計のみ・read-only**。正典(35_link_layer)への stance 列追加・§6/§33 反映、Box upload は **exact owner GO + Wave57 MAP_UPDATE** 待ち(地図ガバナンス遵守)。canonical 昇格 / auto-merge は HOLD(関係は edge / CASE-001)。
