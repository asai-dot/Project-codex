# 久保さんに見せる資料インデックス

- 作成日: 2026-06-08
- 目的: ここまで作った資料（GitHub `docs/` ＋ Box 本体レーン）のうち、**久保さん（ALO Connect 構築・sf_case_id 解決の窓口）に見せるべきもの**を、見せる順に振り分ける。全部出すと埋もれるので「まず見せる／補助／見せない」を明示。

---

## 1. まず見せる（この順）

1. **`docs/13` 受け渡しメッセージ案** — 5/26スキーマ照会(Q2)の続きの送信文。これ自体が久保さん宛のカバー。
2. **ドメイン前提パック v0.2（Box `案件紐付け作業_20260520/`）** ― ALO Connect の resolver に直接効く本体：
   - `HANDOFF_FROM_CODEX_AIFIT_domain_pack_v0.2_20260608.md`（当事者ロール／罠／用語集／統制語彙／confidence機械定義／既知ブロッカー）
   - `AIFIT_goldset_anonymized_v0.2_20260608.csv`（難所37件・expected-output付き）
3. **`docs/01` 進捗ダッシュボード** — 発端の事故→319件全数検証→9パターン→確定40件→全スキャン→DDL、P0〜P7の現在地。**最初の1枚**。
4. **`docs/02` 紐付けロジック** — 9パターン(A〜J,N:M)＋サジェスト照合（どう結び、どう寄せるか）。
5. **`docs/04` 案件/非案件トリアージ** — auto/review/未分類の線引き、「漏れ>過剰」。
6. **`docs/10 §0.5` Box↔SF資産** — `_alo_dir.json`＋DD-BOXCATALOG-001（resolver が何を入力に使うか）。

## 2. 補助（聞かれたら出す）

- `docs/03`（精度を上げる要素）, `docs/05`（設計マップ1枚）, `docs/11`/`docs/12`（難所カタログ／当事者ロール表の解説）, `docs/14`（DIR/メタの置き方・SoT原則）。

## 3. 見せない（内部品質プロセス）

- GPT監査の REQUEST/RESULT 詳細、訂正コミット履歴、WO起票のやり取り。
- → 「**第三者監査(GPT)を通した匿名化版**」という**結論だけ**伝えれば十分。

## 4. 久保さんと合意したいこと（5/26 Q2の実体）

| 優先 | 項目 |
|---|---|
| 高 | a) ゴールドセットを ALO Connect の confidence設計・回帰テストに使ってもらう |
| 高 | c) 「守秘リスクのある判断は auto にしない・人の承認を通す」方針で合意 |
| 中 | b) `party_role` / `non_matter_reason` 統制語彙を ALO Connect 出力と揃える |
| 中 | d) 既知ブロッカー（添付↔Gmail identity bridge 未解決）の扱い |

## 5. 渡し方

| 入口 | 形 |
|---|---|
| `docs/13` の**短縮版（ChatWork）** | まずこれを送る |
| 詳細を聞かれたら | `docs/13` 詳細版 ＋ Box 前提パック v0.2 の共有リンク |

> 注意: AIフィット（社外ベンダー）へ渡るのは**匿名化版のみ**。実データ接続は秘密保持＋権限境界の中で。確定・実装は本体 `案件紐付け作業_20260520/` が正本。
