WORKER_PASS
# Worker Result — W-20260624-120

worker_task_id: W-20260624-120
label: WORKER_PASS

## Summary
v0.1 の event schema を保守的に v0.2 化し、stdlib のみ(jsonschema 非依存)の JSON Schema
draft 2020-12 サブセット検証器 + CLI + pytest を `tools/workflow_event/` に新設した。
v0.2 の examples(5 件・全て架空/PII なし)が schema_v0.2 で全件 green になることと、
ネガティブ例が確実に弾かれることを pytest で固定し、CI に検証ステップを追加した。
実査(SF/Box/Gmail)未完のため、実査由来の大きなスキーマ変更は行わず保守的デルタのみに限定。

## Files Changed (全て allowed_paths 内)
新規:
- docs/workflow_model/v0.2/alo_workflow_event_schema_v0.2.json
- docs/workflow_model/v0.2/alo_workflow_event_examples_v0.2.jsonl
- docs/workflow_model/v0.2/CHANGELOG_v0.2.md  (schema デルタの起票用メモ)
- tools/workflow_event/pyproject.toml
- tools/workflow_event/README.md
- tools/workflow_event/.gitignore
- tools/workflow_event/src/alo_workflow_event/__init__.py
- tools/workflow_event/src/alo_workflow_event/__main__.py
- tools/workflow_event/src/alo_workflow_event/validator.py  (stdlib-only 検証器の本体)
- tools/workflow_event/src/alo_workflow_event/cli.py        (validate サブコマンド)
- tools/workflow_event/tests/conftest.py
- tools/workflow_event/tests/test_validator.py  (サブセット挙動 単体テスト)
- tools/workflow_event/tests/test_examples.py   (examples 全件 green + ネガティブ)
- tools/workflow_event/tests/test_cli.py        (CLI 終了コード/出力)

変更:
- .github/workflows/ci.yml  (tools/worker_queue の隣に tools/workflow_event の pytest ステップを追加)

未改変(読むだけ): docs/workflow_model/v0.1/ 一切触れていない。
注: docs/workflow_model/v0.2/ALO_WORKFLOW_CURRENT_STATE_v0.2.md は当方作成物ではない既存の
未追跡ファイル。allowed_path 内のため放置(改変・削除なし)。

## Commands Run
1) cd tools/workflow_event && PYTHONPATH=src python -m pytest -q
   → `34 passed in 0.10s`
2) CLI 単体:
   PYTHONPATH=tools/workflow_event/src python -m alo_workflow_event validate \
     --schema docs/workflow_model/v0.2/alo_workflow_event_schema_v0.2.json \
     docs/workflow_model/v0.2/alo_workflow_event_examples_v0.2.jsonl
   → `5 checked, 5 passed, 0 failed` / exit=0

## Diff Summary (schema デルタ v0.1 → v0.2)
1. schema_version const: `alo-workflow-event-0.1` → `alo-workflow-event-0.2`。
2. event_type: コア enum(v0.1 の 72 種をそのまま維持)に加え `x-` 接頭辞の自由型を許容する
   緩衝帯化。`oneOf: [{enum:[...72...]}, {pattern: "^x-[a-z0-9_]+$"}]`。未知イベントごとの
   schema 改訂を避けるため。後方互換(コア型を狭めない)。
3. それ以外($defs 構造・enum・required・additionalProperties:false)は v0.1 を維持。
詳細は docs/workflow_model/v0.2/CHANGELOG_v0.2.md。

## Validator サブセット(機械化の本丸)
stdlib のみで type / required / enum / const / additionalProperties:false /
properties / $ref(#/$defs) / oneOf / pattern / items / minItems / minLength /
maxLength / minimum / maximum / format:date-time(緩め)/ format:date(緩め)を解釈。
非対応キーワードは制約なし扱い(保守的=false negative を出さない方向)。

## Remaining Issues / 未確認(queue 起票候補)
- 実査未完: SourceRef.system / object_type の値域確定、FinanceRecord の必須化、
  Box 正本パス命名規約は **未確認**。v0.3 で実査知見を反映する想定(本票スコープ外)。
- draft 2020-12 の一部キーワード(allOf/anyOf/not/$dynamicRef/format の厳密検証等)は
  本サブセット非対応。現スキーマでは未使用のため影響なし。将来スキーマ拡張時に要拡充。

## Next Recommended Task
- 実査(W-20260624-101 系)完了後、実観測に基づき v0.3 schema を起票
  (SourceRef/FinanceRecord/Box パス命名の確定)。本検証器をそのまま再利用可能。
