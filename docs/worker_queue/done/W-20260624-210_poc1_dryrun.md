---
worker_task_id: W-20260624-210
status: queued
priority: P0
owner: claude-code-worker
created_at: 2026-06-24
request: docs/workflow_model/REQUEST_v0.2.md
poc: PoC1
goal: PoC1計測設計(W-200)を接続済みデータ(Google Calendar/Docs, Gmail, Box)で read-only に実証(dry-run)。相談記録を event_id↔doc_id で実サンプル合成し、着手可能4KPIを実測(集計のみ)、受任observed側(Box委任契約書/Gmail受任通知)の検出可否を確認。クロス系突合の限界(SF背骨BLOCKED)を定量化。
mode: implementation
requires_systems:
  - Google Calendar (read-only)
  - Google Drive/Docs (read-only, metadata)
  - Gmail (read-only)
  - Box (read-only)
allowed_paths:
  - docs/workflow_model/v0.2/
allowed_actions:
  - read_google_calendar(read_only)
  - read_google_drive_metadata(read_only)
  - read_gmail(read_only)
  - read_box(read_only)
  - write deliverable
forbidden_actions:
  - any_write_to_external_systems
  - transcribe_privileged_content
  - pii_in_general_artifact
  - ai_estimate_as_human_decision
  - fabricate_cross_links            # 背骨IDが無い突合を推測で確定しない
  - fill_unknown_with_generalities
  - file_move_rename_delete
exit_criteria:
  - 相談記録を実サンプル(10件以上目安)で event_id↔doc_id 合成し、構造(Raw ptr/Canonical メタ)を集計で提示(本文・氏名非転載)
  - 着手可能4KPIのうち単一源で取れるものを実測(集計値=中央値/レンジ/件数。PII無し)
  - 受任observed側(S1 Box委任契約書/S3 Gmail受任通知)の検出可否と件数を集計
  - クロス系突合(相談→見積→契約→受任を1案件として束ねる)の成否と、SF背骨不在による限界を定量化
  - read-only証明・PII非転載(集計/構造/マスクのみ)
  - 測定不能(Notta相談実施時間/MF入金/SF declared突合)は「前提待ち」と明示(捏造禁止)
  - RESULT を done/ または blocked/ に書く
deliverables:
  - docs/workflow_model/v0.2/POC1_dryrun_result_v0.2.md
max_attempts: 2
result_expected_filename: W-20260624-210_RESULT.md
---

# Task — PoC1 実データ dry-run（接続済み4源・read-only・PII非転載）

発注元: `docs/workflow_model/REQUEST_v0.2.md`。設計 = `POC1_measurement_design_v0.2.md`(W-200)。結合 = `PHASE5_consultation_record_survey_v0.2.md`(W-190, event_id↔doc_id)。owner裁定 = `ALO_OWNER_GRILL_ANSWERS_v0.1.md`。前提 = Box正本/SF制御塔/自然発生データ優先/Raw・Canonical・Derived分離。

## 接続とツール（read-only厳守・PII非転載）
`ToolSearch` で読み込む:
- Calendar: `select:mcp__3515fe39-ee8a-4ae7-bba4-aa9ece9299b8__list_events,mcp__3515fe39-ee8a-4ae7-bba4-aa9ece9299b8__get_event`
- Drive: `select:mcp__a80fd983-fa4d-420a-ad56-11d5463d076a__search_files,mcp__a80fd983-fa4d-420a-ad56-11d5463d076a__get_file_metadata`
- Gmail: `select:mcp__e99e6e1f-792c-44ac-8937-6d6ffe74a846__search_threads,mcp__e99e6e1f-792c-44ac-8937-6d6ffe74a846__get_thread`
- Box: `select:mcp__75ca4ee9-f156-4ec1-b970-7370a2392356__search_files_keyword,mcp__75ca4ee9-f156-4ec1-b970-7370a2392356__get_file_details`
- **書込系・本文download系は一切呼ばない**。Gmail/Docsの本文は構造把握(タイムスタンプ・件名様式・有無)に留め、**本文・氏名・事件内容・金額を成果物に転載しない**。

## やること
1. **相談記録の実サンプル合成**: Calendar の相談予定(W-190様式)を10件以上抽出 → 添付fileUrlの doc_id で 事件メモDoc を結合 → consultation_key=event_id の 1相談:N文書 レコードを構築。出力は**構造の集計**(添付有り率・1相談あたりDoc数分布・様式M/G内訳・Raw ptr例はID/様式のみ、氏名非転載)。
2. **着手可能KPIの実測(集計のみ)**:
   - 初回応答時間: Gmail スレッドの受信→初回返信の差(サンプルのスレッド群で中央値/レンジ)。
   - 相談確定時間: 初回接触→相談予定確定までの差(Gmail+Calendar、突合できた範囲で)。
   - 見積送付時間: 相談→見積書(Box)送付(Gmail)までの差(突合できた範囲で)。
   - 契約成立時間: 相談→受任通知/契約(Gmail近似)までの差。
   各KPIは **集計値(件数/中央値/レンジ)** のみ。突合できなかった割合も報告。
3. **受任observed側の検出**: サンプル相談に対し S1=Box委任契約書(search_files_keyword で 委任契約/委任状 等の様式名ヒット件数)・S3=Gmail受任通知(件名様式ヒット件数)の**検出可否と件数**。本文非転載。
4. **クロス系突合の限界定量化**: 相談(Calendar)→見積(Box)→契約(Gmail)→受任 を1案件に束ねる際、**共通背骨ID(SF Matter/Consultation Id)が無い**ため何%が突合でき/できないか。突合は推測で確定しない(fuzzy一致は候補として件数のみ、確定にしない)。これが「SF-ETL/背骨ID整備が要る」根拠になる。
5. **判定**: PoC1設計が接続済み4源で「どこまで自動計測でき/どこから前提(SF-ETL/Notta/MF)待ちか」を結論。

## Do Not
外部システムへ書込まない。**秘匿特権本文・氏名・事件内容・金額を転載しない**(集計/構造/マスクのみ)。**背骨IDの無い突合を推測で確定しない**(候補件数に留める)。未接続(Notta/MF)・BLOCKED(SF)は「前提待ち」と明示。AI推定を人間判断にしない。**git操作なし**。成果物は v0.2 配下のみ。

## 完了処理
RESULT を `done/W-20260624-210_RESULT.md`、1行目 `WORKER_PASS`(exit_criteria充足)。データに到達できなければ該当部を「前提待ち」とし、全体が不能なら blocked/。worker_task_id 記載。
司令塔への戻り値: (a)PASS/BLOCKED (b)成果物パス (c)合成できた相談記録サンプル数と構造集計 (d)実測できたKPIと集計値(PII無) (e)受任observed検出件数 (f)クロス系突合の成功率と背骨ID不在の影響(定量) (g)結論(自動計測の到達点)と前提待ち課題。本文全文・PII本文は貼らない。
